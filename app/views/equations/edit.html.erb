
<div id="edit_form">
  <%= simple_form_for @equation do |f| %>
   <%= f.input :title %>
   <%= f.submit "Save or Press Enter", class: "btn btn-primary" %>
  <% end %>
</div>

<%= link_to 'Back', :back  %>
