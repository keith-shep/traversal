<%= link_to 'Back to equation', equation_path(@step.equation) %>

<h1 class = "mb-2"><%= @step.latex %></h1>
<h3>Comments</h3>
<%= link_to 'Add comment', new_step_comment_path(@step) %>

<ul> <% @step.comments.each do |comment| %>
  <li> <%= comment.message %></li>
  <ul>
    <li> created on <%= comment.created_at %></li>
    <li> <%= link_to 'Delete', delete_comment_path(comment), method: :delete %> </li>
  </ul>
  <% end %>
</ul>
